# scripts

| Script | Que hace |
|---|---|
| `bootstrap.ps1` | Instalacion inicial: copia los `.env`, crea el venv e instala dependencias. Una sola vez. |
| `dev.ps1` | Levanta docker, backend y frontend en terminales separadas. Uso diario. |
