DATABASE_URL=postgresql+psycopg://gabame:cambiar_en_local@localhost:5433/clientes_gabame
SECRET_KEY=generar_con_openssl_rand_hex_32
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=14
BACKEND_CORS_ORIGINS=http://localhost:3000
ENVIRONMENT=local
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM=no-reply@localhost
FRONTEND_URL=http://localhost:3000
# Farmacias GABAME: dominio definitivo o el temporal mientras la tienda no este publicada
URL_FARMACIAS=https://farmaciasgabame.com
EMAIL_TOKEN_EXPIRE_HOURS=24
RESET_TOKEN_EXPIRE_MINUTES=60
# smtp (Mailhog en local) | memoria (solo pruebas)
EMAIL_MODO=smtp
# Documentos de partners (disco local en dev, fuera del repo)
UPLOADS_DIR=uploads
UPLOAD_MAX_MB=10
